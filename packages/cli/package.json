{
  "name": "@cachette/cli",
  "version": "0.0.1",
  "private": false,
  "bin": {
    "cachette": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "pnpm --filter @cachette/registry build && tsc -p tsconfig.build.json",
    "test": "pnpm build && node test/cli.test.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cachette/registry": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.20.1"
  }
}
