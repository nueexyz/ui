import { LocalizedMarkdown } from "../../documentation/LocalizedMarkdown";
import { CodeBlock } from "../../documentation/CodeBlock";
import DefaultSource from "./Default.tsx?raw";
import { Meta } from "@storybook/addon-docs/blocks";
import { ComponentPreview } from "../../documentation/ComponentPreview";

<Meta title="Components/Button Group" />

# Button Group

<LocalizedMarkdown text={"Group actions that serve the same purpose into one control."} />

<ComponentPreview name="button-group--Default" source={DefaultSource} />

<LocalizedMarkdown text={"## Installation"} />

<CodeBlock code="pnpm dlx @nuee/ui add button-group" language="bash" />

<LocalizedMarkdown text={"## Usage"} />

```tsx
import { Button } from "@/components/ui/button";
import { ButtonGroup } from "@/components/ui/button-group";
import { Icon } from "@/components/ui/icon";

<div>
  <ButtonGroup aria-label="Navigate back">
    <Button size="icon-sm" variant="secondary" aria-label="Go back">
      <Icon aria-hidden="true" name="chevronLeft" />
    </Button>
  </ButtonGroup>

  <ButtonGroup aria-label="Message actions">
    <Button size="sm" variant="secondary">
      Archive
    </Button>
    <Button size="sm" variant="secondary">
      Report
    </Button>
  </ButtonGroup>

  <ButtonGroup aria-label="More actions">
    <Button size="sm" variant="secondary">
      Snooze
    </Button>
    <Button size="icon-sm" variant="secondary" aria-label="More actions">
      <Icon aria-hidden="true" name="moreHorizontal" />
    </Button>
  </ButtonGroup>
</div>;
```
