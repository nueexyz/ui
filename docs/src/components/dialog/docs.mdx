import { LocalizedMarkdown } from "../../documentation/LocalizedMarkdown";
import { CodeBlock } from "../../documentation/CodeBlock";
import DefaultSource from "./Default.tsx?raw";
import { Meta } from "@storybook/addon-docs/blocks";
import { ComponentPreview } from "../../documentation/ComponentPreview";

<Meta title="Components/Dialog" />

# Dialog

<LocalizedMarkdown text={"Temporarily pause the current flow for confirmation or input."} />

<ComponentPreview name="dialog--Default" source={DefaultSource} isolated height={360} />

<LocalizedMarkdown text={"## Installation"} />

<CodeBlock code="pnpm dlx @nuee/ui add dialog" language="bash" />

<LocalizedMarkdown text={"## Usage"} />

```tsx
import { Dialog, DialogContent, DialogTitle, DialogTrigger } from "@/components/ui/dialog";

<Dialog>
  <DialogTrigger>Open profile</DialogTrigger>
  <DialogContent>
    <DialogTitle>Profile</DialogTitle>
  </DialogContent>
</Dialog>;
```
