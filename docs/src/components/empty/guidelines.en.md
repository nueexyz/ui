## Usage guidelines

Distinguish a new account, no search matches, and unavailable data. Each needs a different explanation and action.

> A failed request is not an empty result; offer recovery instead of inviting users to create duplicate data.
