import { LocalizedMarkdown } from "../../documentation/LocalizedMarkdown";
import { CodeBlock } from "../../documentation/CodeBlock";
import DefaultSource from "./Default.tsx?raw";
import { Meta } from "@storybook/addon-docs/blocks";
import { ComponentPreview } from "../../documentation/ComponentPreview";

<Meta title="Components/Empty" />

# Empty

<LocalizedMarkdown text={"Explain why there is no content yet and what to do next."} />

<ComponentPreview name="empty--Default" source={DefaultSource} />

<LocalizedMarkdown text={"## Installation"} />

<CodeBlock code="pnpm dlx @nuee/ui add empty" language="bash" />

<LocalizedMarkdown text={"## Usage"} />

```tsx
import { Button } from "@/components/ui/button";
import {
  Empty,
  EmptyContent,
  EmptyDescription,
  EmptyHeader,
  EmptyMedia,
  EmptyTitle,
} from "@/components/ui/empty";
import { Icon } from "@/components/ui/icon";

<Empty>
  <EmptyHeader>
    <EmptyMedia>
      <Icon aria-hidden="true" name="folder" />
    </EmptyMedia>
    <EmptyTitle>No saved projects</EmptyTitle>
    <EmptyDescription>Save projects you visit often to open them here quickly.</EmptyDescription>
  </EmptyHeader>
  <EmptyContent>
    <Button>Save project</Button>
  </EmptyContent>
</Empty>;
```
