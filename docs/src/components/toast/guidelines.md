## 사용 가이드

### 사용할 때

보관함으로 이동한 결과처럼 현재 작업을 벗어나지 않고 완료를 알릴 때 사용합니다. 계속 보여야 하는 상태는 [Banner](?path=/docs/components-banner--docs), 값 수정은 `FieldError`를 사용합니다. 작업을 막는 실패를 토스트에만 남기지 않습니다.

### 행동과 유지할 정보

제목에는 결과를 씁니다. 설명은 결과의 영향이나 다음 행동을 알릴 때만 추가합니다. Undo는 실제로 데이터를 복원하는 처리와 연결합니다. 알림을 닫는 것은 작업을 되돌리는 것이 아닙니다.

진행 중인 작업과 그 결과는 알림 하나의 상태로 이어 줍니다. 각각 별도 알림을 추가해 경쟁하게 하지 않습니다. 높은 우선순위는 읽고 있는 안내를 중단할 만큼 시급한 정보에만 사용합니다. 아이콘이 빨간색이라는 이유로 우선순위를 높이지 않습니다.

### 닫기

> Escape는 가장 최근 알림부터 하나씩 닫습니다. Dismiss all은 요청이 아니라 알림을 지웁니다. 필요한 재시도나 복구 행동은 토스트가 사라진 뒤에도 페이지에서 사용할 수 있게 합니다. `clearAllProps.children`에 제품 문구를 지정합니다.
