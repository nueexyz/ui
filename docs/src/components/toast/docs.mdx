import overview from "./overview.md?raw";
import overviewEnglish from "./overview.en.md?raw";
import { Guideline } from "../../documentation/Guideline";
import guidelines from "./guidelines.md?raw";
import english from "./guidelines.en.md?raw";
import { LocalizedMarkdown } from "../../documentation/LocalizedMarkdown";
import { CodeBlock } from "../../documentation/CodeBlock";
import StatesSource from "./States.tsx?raw";
import MultipleNotificationsSource from "./MultipleNotifications.tsx?raw";
import ActionsSource from "./Actions.tsx?raw";
import DefaultSource from "./Default.tsx?raw";
import { Meta } from "@storybook/addon-docs/blocks";
import { ComponentPreview } from "../../documentation/ComponentPreview";

<Meta title="Components/Toast" />

<Guideline content={overview} english={overviewEnglish} />

<LocalizedMarkdown text={"## Basic example"} />

<ComponentPreview name="toast--Default" source={DefaultSource} isolated height={420} />

<LocalizedMarkdown text={"## Composition"} />

```tsx
import { Button } from "@/components/ui/button";
import { toast, Toaster } from "@/components/ui/toast";

export function ToastDemo() {
  return (
    <>
      <Button onClick={() => toast.add({ title: "Saved." })}>Save</Button>
      <Toaster clearAllProps={{ children: "Dismiss all" }} />
    </>
  );
}
```

<LocalizedMarkdown text={"## Installation"} />

<CodeBlock code="pnpm dlx @nuee/ui add toast" language="bash" />

<LocalizedMarkdown text={"Mount one `Toaster` near the root of your application."} />

<Guideline content={guidelines} english={english} />

<LocalizedMarkdown text={"## More examples"} />

<LocalizedMarkdown text={"### Actions"} />

<LocalizedMarkdown
  text={
    "Use an action for reversible work. This example closes the notification when\nUndo is pressed; in your application, restore the archived item in that handler.\nUse `toast.promise()` to follow a pending operation through success or failure."
  }
/>

<ComponentPreview name="toast--Actions" source={ActionsSource} isolated height={420} />

<LocalizedMarkdown text={"### Multiple notifications"} />

<LocalizedMarkdown
  text={
    "The latest three notifications stack. Hover over the stack to expand it.\nPress Escape to dismiss one notification at a time, newest first.\nCustomize the clear-all label with `clearAllProps.children`."
  }
/>

<ComponentPreview
  name="toast--MultipleNotifications"
  source={MultipleNotificationsSource}
  isolated
  height={420}
/>

<LocalizedMarkdown text={"### States"} />

<LocalizedMarkdown
  text={
    "Combine icons and text to distinguish success, information, warnings, and errors.\nUse high priority for errors that need immediate attention."
  }
/>

<ComponentPreview name="toast--States" source={StatesSource} isolated height={420} />
