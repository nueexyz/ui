## Usage guidelines

Use it for a persistent mode such as bold formatting. Use `Button` for a one-time command and `Switch` for a setting.

> Keep the accessible state in sync with the visible pressed treatment.
