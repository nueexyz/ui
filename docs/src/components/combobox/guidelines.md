## 사용 가이드

### 사용할 때

사람이나 프로젝트처럼 글자를 입력해 후보를 좁힌 뒤 항목을 선택할 때 사용합니다. 목록을 훑는 것으로 충분하면 [Select](?path=/docs/components-select--docs), 자유로운 텍스트 값이면 `Input`을 사용합니다.

### 검색과 선택 구분

입력 중인 검색어와 확정된 선택값을 구분합니다. 임의의 글자를 입력하면 새 항목이 만들어진다고 안내하지 않습니다. 새 항목을 만들려면 애플리케이션에서 생성 행동과 저장 처리를 제공해야 합니다. 비슷한 이름을 구별할 수 있도록 후보 이름을 구체적으로 표시합니다.

### 결과와 상태

검색이 끝났지만 일치하는 항목이 없을 때 `ComboboxEmpty`를 사용합니다. 서버에서 후보를 가져온다면 로딩·실패·오래된 응답은 애플리케이션에서 처리합니다. 빈 목록만으로 세 상태를 모두 설명하지 않습니다. 실패 후에도 검색어를 유지해 다시 시도할 수 있게 합니다.

> 플레이스홀더와 별개로 입력에 접근 가능한 이름을 제공합니다. 포인터 선택뿐 아니라 키보드 선택과 값 지우기도 확인합니다. 결과 페이지의 동작은 [Search and Filter](?path=/docs/patterns-search-and-filter--docs)를 확인합니다.
