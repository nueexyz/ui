# Radio Group

Let people compare mutually exclusive choices before selecting one.

Keep choices visible when their differences matter. Use `Select` when a compact list is sufficient.

> If users must clear an optional selection, provide an explicit “None” choice.
