## Usage guidelines

Use `Input` for a single line, `Textarea` for several lines, and selection controls when values come from a fixed set.

> A placeholder disappears during entry; keep the field's name visible.
