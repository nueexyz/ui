## Usage guidelines

Name the destination or resource. Use `Button` when the purpose is performing an action rather than navigating.

> Avoid repeated “Click here” labels that become indistinguishable outside their paragraphs.
