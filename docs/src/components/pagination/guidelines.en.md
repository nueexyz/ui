## Usage guidelines

Use numbered pages when position matters. Keep the current page distinct from available destinations.

> When filters change the result set, reset or validate the current page instead of leaving users on an empty page.
