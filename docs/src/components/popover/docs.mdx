import { LocalizedMarkdown } from "../../documentation/LocalizedMarkdown";
import { CodeBlock } from "../../documentation/CodeBlock";
import DefaultSource from "./Default.tsx?raw";
import { Meta } from "@storybook/addon-docs/blocks";
import { ComponentPreview } from "../../documentation/ComponentPreview";

<Meta title="Components/Popover" />

# Popover

<LocalizedMarkdown
  text={"Provide supporting information or simple controls without leaving the current screen."}
/>

<LocalizedMarkdown text={"**Alignment**"} />

<LocalizedMarkdown
  text={"Align to the trigger’s left edge by default, or center and right as needed."}
/>

<ComponentPreview name="popover--Default" source={DefaultSource} isolated height={360} />

<LocalizedMarkdown text={"## Installation"} />

<CodeBlock code="pnpm dlx @nuee/ui add popover" language="bash" />

<LocalizedMarkdown text={"## Usage"} />

```tsx
import { Button } from "@/components/ui/button";
import {
  Popover,
  PopoverContent,
  PopoverDescription,
  PopoverHeader,
  PopoverTitle,
  PopoverTrigger,
} from "@/components/ui/popover";

<Popover>
  <PopoverTrigger render={<Button variant="secondary">Align left</Button>} />
  <PopoverContent>
    <PopoverHeader>
      <PopoverTitle>Align left</PopoverTitle>
      <PopoverDescription>Align the popover start with the trigger.</PopoverDescription>
    </PopoverHeader>
  </PopoverContent>
</Popover>;
```
