# Select

Keep a fixed choice compact while leaving the selected value visible.

Use it when reading the list is enough to recognize an option. Use `Combobox` when typing makes selection easier.

> Keep the field name separate from the selected value.
