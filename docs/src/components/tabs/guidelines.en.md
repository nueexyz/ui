## Usage guidelines

Use short labels that distinguish peer views. Use navigation links when each destination is a separate page.

> Do not use tabs as numbered steps when one step depends on completing another.
