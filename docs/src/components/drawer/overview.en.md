# Drawer

Keep a supporting task in an edge panel with space suited to its content.

Choose a `Drawer` for filters or focused supporting work. Decide whether the background should remain interactive before choosing an edge.

> A `Drawer` is modal by default; sliding in from the side does not make the page interactive.
