import { LocalizedMarkdown } from "../../documentation/LocalizedMarkdown";
import { CodeBlock } from "../../documentation/CodeBlock";
import SizesSource from "./Sizes.tsx?raw";
import VariantsSource from "./Variants.tsx?raw";
import ActionAlignmentSource from "./ActionAlignment.tsx?raw";
import DefaultSource from "./Default.tsx?raw";
import { Meta } from "@storybook/addon-docs/blocks";
import { ComponentPreview } from "../../documentation/ComponentPreview";

<Meta title="Components/Content Row" />

# Content Row

<LocalizedMarkdown
  text={"Group information, supporting text, and related actions into a repeatable row."}
/>

<LocalizedMarkdown text={"**List**"} />

<LocalizedMarkdown text={"Present related items with consistent spacing and alignment."} />

<ComponentPreview name="content-row--Default" source={DefaultSource} />

<LocalizedMarkdown text={"## Installation"} />

<CodeBlock code="pnpm dlx @nuee/ui add content-row" language="bash" />

<LocalizedMarkdown text={"## Usage"} />

```tsx
import { DotsThreeIcon, FolderIcon } from "@phosphor-icons/react";
import { Button } from "@/components/ui/button";
import {
  ContentRow,
  ContentRowActions,
  ContentRowContent,
  ContentRowDescription,
  ContentRowGroup,
  ContentRowMedia,
  ContentRowTitle,
} from "@/components/ui/content-row";

<ContentRowGroup>
  <ContentRow variant="outline">
    <ContentRowMedia>
      <FolderIcon aria-hidden="true" />
    </ContentRowMedia>
    <ContentRowContent>
      <ContentRowTitle>Design system</ContentRowTitle>
      <ContentRowDescription>Last edited today at 2:18 PM</ContentRowDescription>
    </ContentRowContent>
    <ContentRowActions>
      <Button aria-label="More actions" size="icon-sm" variant="ghost">
        <DotsThreeIcon aria-hidden="true" size="1rem" />
      </Button>
    </ContentRowActions>
  </ContentRow>
</ContentRowGroup>;
```

<LocalizedMarkdown text={"## Examples"} />

<LocalizedMarkdown text={"### Action alignment"} />

<LocalizedMarkdown
  text={
    "Titles and action labels share their first baseline across button sizes. Descriptions can wrap without changing that alignment."
  }
/>

<ComponentPreview name="content-row--ActionAlignment" source={ActionAlignmentSource} />

<LocalizedMarkdown text={"### Variants"} />

<LocalizedMarkdown
  text={"Choose a surface that fits the row’s grouping and emphasis in its parent layout."}
/>

<ComponentPreview name="content-row--Variants" source={VariantsSource} />

<LocalizedMarkdown text={"### Sizes"} />

<LocalizedMarkdown
  text={"Choose a row density that matches the amount of information in the list."}
/>

<ComponentPreview name="content-row--Sizes" source={SizesSource} />
