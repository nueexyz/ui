## Usage guidelines

Use it for items people scan individually. Use `Table` when comparing several attributes across rows.

> Let the row handle first-line alignment instead of adding padding to compensate for each description.
