# Message

Keep the sender, content, and delivery context together so conversations remain understandable.

Use `Message` for a conversation entry and `Bubble` for its visual message body. Use `Banner` for page-level notices.

> `Message` is not a generic feedback container for form errors or page warnings.
