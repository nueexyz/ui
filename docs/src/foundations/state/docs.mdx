import { ComponentPreview } from "../../documentation/ComponentPreview";
import source from "./InteractionExample.tsx?raw";
import { Interaction, FeedbackColors } from "../color/Reference";
import { Opacity } from "./Reference";
import { LocalizedMarkdown } from "../../documentation/LocalizedMarkdown";
import { Meta } from "@storybook/addon-docs/blocks";
import { Guideline } from "../../documentation/Guideline";
import guidelines from "./guidelines.md?raw";
import english from "./guidelines.en.md?raw";

<Meta title="Foundations/State" />

<Guideline content={guidelines} english={english} />

<ComponentPreview name="state--InteractionExample" source={source} />

<LocalizedMarkdown text={"## Token reference"} />

<LocalizedMarkdown text={"### Interaction colors"} />

<LocalizedMarkdown
  text={
    "Interaction layers show hover, press, selection, and availability. Translucent tokens are previewed over a surface; solid-action layers use the primary action background."
  }
/>

<Interaction />

<LocalizedMarkdown text={"### Feedback colors"} />

<LocalizedMarkdown
  text={
    "Feedback colors describe an outcome, not pointer interaction. Compare the background, content, and border tokens for information, success, warning, and error."
  }
/>

<FeedbackColors />

<LocalizedMarkdown text={"### Opacity"} />

<LocalizedMarkdown
  text={
    "The swatch shows opacity on its own. The disabled Button uses disabled colors and behavior without reducing the label's opacity again."
  }
/>

<Opacity />
